name: Release Connection Path Fix 1.3.4

on:
  push:
    branches: [agent/wpai-production-rebuild]
    paths:
      - 'scripts/release_connection_path_fix_1_3_4.py'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/wpai-production-rebuild
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply synchronized 1.3.4 version
        run: python scripts/release_connection_path_fix_1_3_4.py
      - name: Verify locked source and application gates
        run: |
          npm ci --no-audit --no-fund
          npm run check
      - name: Commit verified release source
        run: |
          rm -f scripts/release_connection_path_fix_1_3_4.py .github/workflows/release-connection-path-fix-1.3.4.yml
          git config user.name 'WPAI Build Agent'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'release: WPAI 1.3.4 Windows path repair'
          git push origin HEAD:agent/wpai-production-rebuild
